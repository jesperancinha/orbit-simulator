<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>jfse</groupId>
    <artifactId>orbit-simulator-app</artifactId>
    <packaging>jar</packaging>
    <version>1.0.0-SNAPSHOT</version>
    <name>orbit-simulator-app</name>
    <url>http://maven.apache.org</url>

    <properties>
        <kart.race.laps.api>1.0.0-SNAPSHOT</kart.race.laps.api>
        <maven.compiler>3.5.1</maven.compiler>
        <java.version>1.8</java.version>
        <junit>4.12</junit>
        <jmock.junit4>2.8.2</jmock.junit4>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
        </plugins>
    </build>
    <dependencies>
        <dependency>
            <groupId>jfse</groupId>
            <artifactId>orbit-simulator-api</artifactId>
            <version>${kart.race.laps.api}</version>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.jmock</groupId>
            <artifactId>jmock-junit4</artifactId>
            <version>${jmock.junit4}</version>
        </dependency>
    </dependencies>
</project>
